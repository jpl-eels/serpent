# Label (optional)
string label
# Min value (valid if count > 0)
float64 min
# Max value (valid if count > 0)
float64 max
# Mean or expectation of the random variable E[X]
float64 mean
# Variance of the random variable Var[X] = E[(X - E[X])^2]
float64 variance
# Number of measurements used to compute statistics (use 0 if measurements were not used)
uint64 count
